struct RGB {
  byte r;
  byte g;
  byte b;
};
 
// Define some colors we'll use frequently
RGB white = { 255, 255, 255 };
RGB red = { 255, 0, 0 };
RGB green ={ 0, 255,  };
RGB blue = { 0, 0, 255 };

RGB violet = { 255, 0, 255 };

RGB off = { 0, 0, 0 };
